Write the ultimate title for an article in <%= language %> that will make the reader want to read the article. 
An initial version of the title is "<%= title %>".
<% if (keywords) { %>
    Some relevant key words are: "<%= keywords %>".
<% } %>