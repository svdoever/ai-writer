Write the ultimate title for the article that will make the reader want to read the article. 
A "raw" version of the title is “<%= title %>”.
<% if (keywords) { %>Some relevant key words are “<%= keywords %>”.
<% } %>